/**
 * All legal documents use the same responsive, deterministic content offset.
 * Keep this literal centralized so aliases and CMS-backed pages cannot drift.
 */
export const LEGAL_CONTENT_SECTION_CLASS =
  'relative min-h-[60vh] overflow-hidden py-[clamp(1.5rem,3vw,3rem)]';
