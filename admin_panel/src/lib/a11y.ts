/** Shared focus ring for admin custom controls (shadcn Button/Input already include focus). */
export const FOCUS_RING =
  'focus-visible:outline-none focus-visible:ring-[3px] focus-visible:ring-ring/50 focus-visible:border-ring';

/** Auth form fields (login panel). */
export const AUTH_FIELD_CLS =
  'w-full rounded-lg border border-[var(--brand-gold-border)] bg-white/50 py-5 transition-all focus:border-[var(--brand-gold)] focus:outline-none';
